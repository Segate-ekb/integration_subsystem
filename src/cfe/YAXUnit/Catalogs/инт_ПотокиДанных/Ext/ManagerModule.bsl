&Вместо("СформироватьСообщениеПоПотоку")
Функция Мок_СформироватьСообщениеПоПотоку(ИсходныеДанные, ПотокДанных)
  ПараметрыМетода = Мокито.МассивПараметров(ИсходныеДанные, ПотокДанных);
  ПрерватьВыполнение = Ложь;

  Результат = МокитоПерехват.АнализВызова(Справочники.инт_ПотокиДанных, "СформироватьСообщениеПоПотоку", ПараметрыМетода, ПрерватьВыполнение);

  Если Не ПрерватьВыполнение Тогда
    Возврат ПродолжитьВызов(ИсходныеДанные, ПотокДанных);
  Иначе
    Возврат Результат;
  КонецЕсли;
КонецФункции

&Вместо("ОбработатьВходящееСообщениеПоПотоку")
Процедура Мок_ОбработатьВходящееСообщениеПоПотоку(СоответствиеСообщения, ПотокДанных, РазрешитьФиксациюИзменений = Истина)
  ПараметрыМетода = Мокито.МассивПараметров(СоответствиеСообщения, ПотокДанных, РазрешитьФиксациюИзменений);
  ПрерватьВыполнение = Ложь;

  МокитоПерехват.АнализВызова(Справочники.инт_ПотокиДанных, "ОбработатьВходящееСообщениеПоПотоку", ПараметрыМетода, ПрерватьВыполнение);

  Если Не ПрерватьВыполнение Тогда
    ПродолжитьВызов(СоответствиеСообщения, ПотокДанных, РазрешитьФиксациюИзменений);
  КонецЕсли;
КонецПроцедуры
