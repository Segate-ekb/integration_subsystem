&Вместо("СформироватьСообщениеПоПодписчику")
Функция Мок_СформироватьСообщениеПоПодписчику(ДанныеСообщения, Подписчик, ИдентификаторСообщения)
  ПараметрыМетода = Мокито.МассивПараметров(ДанныеСообщения, Подписчик, ИдентификаторСообщения);
  ПрерватьВыполнение = Ложь;

  Результат = МокитоПерехват.АнализВызова(Справочники.инт_Подписчики, "СформироватьСообщениеПоПодписчику", ПараметрыМетода, ПрерватьВыполнение);

  Если Не ПрерватьВыполнение Тогда
    Возврат ПродолжитьВызов(ДанныеСообщения, Подписчик, ИдентификаторСообщения);
  Иначе
    Возврат Результат;
  КонецЕсли;
КонецФункции

&Вместо("ОтправитьСообщение")
Процедура Мок_ОтправитьСообщение(Сообщение)
  ПараметрыМетода = Мокито.МассивПараметров(Сообщение);
  ПрерватьВыполнение = Ложь;

  МокитоПерехват.АнализВызова(Справочники.инт_Подписчики, "ОтправитьСообщение", ПараметрыМетода, ПрерватьВыполнение);

  Если Не ПрерватьВыполнение Тогда
    ПродолжитьВызов(Сообщение);
  КонецЕсли;
КонецПроцедуры
