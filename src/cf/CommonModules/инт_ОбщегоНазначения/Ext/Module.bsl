Функция ЗначениеРеквизитаОбъекта(Ссылка, ИмяРеквизита) Экспорт
	
	Результат = ЗначенияРеквизитовОбъекта(Ссылка, ИмяРеквизита);
	Возврат Результат[ИмяРеквизита];
	
КонецФункции

Функция ЗначенияРеквизитовОбъекта(Ссылка, Знач Реквизиты) Экспорт
	
	ПолноеИмяОбъектаМетаданных = Ссылка.Метаданные().ПолноеИмя();
	
	ШаблонТекстаЗапроса = "ВЫБРАТЬ %1 ИЗ %2 КАК Таблица ГДЕ Таблица.Ссылка = &Ссылка";
	ТекстЗапроса = СтрШаблон(ШаблонТекстаЗапроса, Реквизиты, ПолноеИмяОбъектаМетаданных);
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("Ссылка", Ссылка);
	Запрос.Текст = ТекстЗапроса;
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Результат = Новый Структура(Реквизиты);
	
	Если Выборка.Следующий() Тогда
		ЗаполнитьЗначенияСвойств(Результат, Выборка);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Дополняет структуру значениями из другой структуры.
//
// Параметры:
//   Приемник - Структура - коллекция, в которую будут добавляться новые значения.
//   Источник - Структура - коллекция, из которой будут считываться пары Ключ и Значение для заполнения.
//   Заменять - Булево
//            - Неопределено - что делать в местах пересечения ключей источника и приемника:
//                             Истина - заменять значения приемника (самый быстрый способ),
//                             Ложь   - не заменять значения приемника (пропускать),
//                             Неопределено - значение по умолчанию. Бросать исключение.
//
Процедура ДополнитьСтруктуру(Приемник, Источник, Заменять = Неопределено) Экспорт
	
	Для Каждого Элемент Из Источник Цикл
		Если Заменять <> Истина И Приемник.Свойство(Элемент.Ключ) Тогда
			Если Заменять = Ложь Тогда
				Продолжить;
			Иначе
				ВызватьИсключение СтрШаблон(
					НСтр("ru = 'Пересечение ключей источника и приемника: ""%1"".'"),
					Элемент.Ключ);
			КонецЕсли
		КонецЕсли;
		Приемник.Вставить(Элемент.Ключ, Элемент.Значение);
	КонецЦикла;
	
КонецПроцедуры

// Создает массив и помещает в него переданное значение.
//
// Параметры:
//  Значение - Произвольный - любое значение.
//
// Возвращаемое значение:
//  Массив - массив из одного элемента.
//
Функция ЗначениеВМассиве(Знач Значение) Экспорт
	
	Результат = Новый Массив;
	Результат.Добавить(Значение);
	Возврат Результат;
	
КонецФункции

// Функция - Время базы
// Текущий момент в пространстве, в котором подсистема хранит и сравнивает моменты времени, -
// в местном времени часового пояса информационной базы. Часовой пояс сеанса на результат
// не влияет: сеанс может установить себе любой, записываемые моменты от этого не сместятся.
//
// Возвращаемое значение:
//  Дата - Текущий момент во времени базы
//
Функция ВремяБазы() Экспорт
	
	Возврат МестноеВремя(ТекущаяУниверсальнаяДата(), ЧасовойПоясВремениБазы());
	
КонецФункции

// Функция - Часовой пояс времени базы
// Часовой пояс, в котором подсистема хранит моменты. Нужен там, где сохранённый момент
// приводится к другому пространству, - на границе с внешним миром.
//
// Возвращаемое значение:
//  Строка - Идентификатор часового пояса
//
Функция ЧасовойПоясВремениБазы() Экспорт
	
	Возврат ЧасовойПоясХраненияМоментов(ПолучитьЧасовойПоясИнформационнойБазы());
	
КонецФункции

// Функция - Часовой пояс хранения моментов
// Разрешает часовой пояс хранения по часовому поясу информационной базы. После создания базы
// её часовой пояс не определён, и тогда платформа молча взяла бы часовой пояс компьютера:
// в кластере из нескольких серверов приложений моменты записывались бы в разных поясах,
// а пауза между попытками смещалась бы на их разницу. Поэтому неопределённость разрешается
// явно универсальным временем - оно одинаково на всех серверах кластера.
//
// О самой неопределённости сообщает шаг обновления информационной базы подсистемы метрик:
// незаданный часовой пояс - неверная настройка, а не режим работы.
//
// Параметры:
//  ЧасовойПоясБазы - Строка, Неопределено - Часовой пояс информационной базы
//
// Возвращаемое значение:
//  Строка - Идентификатор часового пояса, в котором хранятся моменты
//
Функция ЧасовойПоясХраненияМоментов(ЧасовойПоясБазы) Экспорт
	
	Возврат ?(ЗначениеЗаполнено(ЧасовойПоясБазы), ЧасовойПоясБазы, "UTC");
	
КонецФункции
