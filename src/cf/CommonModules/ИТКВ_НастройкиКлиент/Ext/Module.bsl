#Область ПрограммныйИнтерфейс

Функция ОбработкаОповещения(Форма, ИмяСобытия, ВидИнструмента = Неопределено) Экспорт
	
	Если ИмяСобытия = ИТКВ_НастройкиКлиентСервер.ИмяСобытияНастройкиИзменены() Тогда
		
		ВсеНастройки = ИТКВ_НастройкиВызовСервера.ЗагрузитьВсе(ВидИнструмента);
		ЗаполнитьЗначенияСвойств(Форма, ВсеНастройки);
		
		Результат = Истина;
		
	Иначе
		
		Результат = Ложь;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЗначениеОбщейНастройки(Форма, Имя) Экспорт
	
	Возврат ИТКВ_НастройкиКлиентСервер.ЗначениеОбщейНастройки(Форма, Имя);
	
КонецФункции

Функция ЗначениеНастройки(Форма, Имя) Экспорт
	
	Возврат ИТКВ_НастройкиКлиентСервер.ЗначениеНастройки(Форма, Имя);
	
КонецФункции

#КонецОбласти
